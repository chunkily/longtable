- Add in an eraser tool to remove drawings.
- Highlight selected color with a light blue focus so that if black is selected it is visible
- Add a toggle to allow setting a fill on rectangle and circle shapes
- Add a range input to select stroke sizes
- Make drawing controls like color selection, fill and range input to only show when the relevant tool is selected.
- Drawing should not occur when user uses the right mouse button.
- Allow for users to undo their moves and drawing edits.
- Change from uploading maps and token images for every single instance to instead pick from an asset library. Asset library on a separate route (maybe /assets) accessible by all users. Add in an optional attribution and license metadata field for each asset (perhaps just a single free text field instead of two separate ones?).
- On opening the application for the first time on their device, the user is prompted for their display name (which they can change later). This is re-used across all rooms. Duplicate names are allowed.
- Add measuring tools. Distance is measured using the rule of first diagonal movement is 1, second diagonal movement is 2.
- Move chat into a panel on the right under a chat tab. Have separate tabs for event logs, initiative tracker.
- Add controls to make it easy to track damage / hp / conditions on individual tokens.
- Add branding in the form of an favicon and a stylized title.
- GM can delete chat messages. Users can also delete their own messages.
- Measuring tool for area of effects (cones / spheres) where the affected area originates from the current mouse location.
- Display all connected players in the chat area.
- Add a button to leave the room.
